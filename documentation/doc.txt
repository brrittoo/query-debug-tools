Local Development (Full Source Publish)

Modify the package inside your Laravel project:


Install/update locally:
composer update brrittoo/url-encoder


✅ Changes take effect immediately.

Local Development (From Git Clone)

Clone outside your Laravel project:

git clone https://github.com/brrittoo/laravel-url-encoder.git ../debug-tools


Add as a path repository:

"repositories": [
    {
        "type": "path",
        "url": "../debug-tools",
        "options": { "symlink": true }
    }
]


Require locally:
composer require brrittoo/url-encoder:@dev
Publish configuration:

php artisan vendor:publish --provider="Brrittoo\UrlEncoder\UrlEncoderServiceProvider" --tag="url-encoder-config"





✅ Symlinks the clone; changes reflect immediately.

Contributing
Fork the repository
Create a feature branch:
git checkout -b feature/MyFeature


Commit changes:
git commit -m "Add new feature"

Push to your fork:
git push origin feature/MyFeature


Submit a Pull Request

We review contributions before merging.

Author
Ataul Galib
Email: ataul.gonigalib@gmail.com

🔗 Repository: https://github.com/ataulgalib/query-debug



